use std::any::Any;

pub struct Memory {
}

impl Memory {
    fn remember(&mut self, path: Vec<&str>) {
	}
    fn learn(&mut self, path: Vec<&str>, x: &Any) {
	}
    fn forget(&mut self, path: Vec<&str>) {
	}
}
